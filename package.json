{
  "name": "rocket-shop",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "deploy": "DEPLOY_HOSTNAME=galaxy.meteor.com meteor deploy alexrocketshop --settings settings.json"
  },
  "dependencies": {
    "meteor-node-stubs": "~0.2.0"
  }
}
